name: Android Build

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install
        working-directory: ./Calculadora-de-Tablas

      - name: Build Web App
        run: npm run build
        working-directory: ./Calculadora-de-Tablas

      - name: Sync Capacitor
        run: npx cap sync
        working-directory: ./Calculadora-de-Tablas

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      
      - name: Decode Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/my-release-key.keystore
        working-directory: ./Calculadora-de-Tablas

      - name: Make Gradlew Executable
        run: chmod +x android/gradlew
        working-directory: ./Calculadora-de-Tablas

      - name: Build Android Bundle (Release - Signed)
        run: |
          cd android
          ./gradlew bundleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/app/my-release-key.keystore \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}
        working-directory: ./Calculadora-de-Tablas

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: app-release-signed
          path: Calculadora-de-Tablas/android/app/build/outputs/bundle/release/app-release.aab
