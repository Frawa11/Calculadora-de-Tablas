import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frawa11.calculadoradetablas',
  appName: 'Calculadora de Tablas',
  webDir: 'dist'
};

export default config;
